{
  "name": "@repo/tests",
  "module": "index.ts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "tsup && node --test"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "latest",
    "tsup": "^8.1.0"
  },
  "dependencies": {
    "try-handler": "workspace:*"
  }
}
