{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "reset": "turbo build --filter lets-try && turbo test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "test": "turbo build && turbo test --filter @repo/tests"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^2.0.14"
  },
  "packageManager": "pnpm@8.15.6",
  "name": "with-vite"
}
